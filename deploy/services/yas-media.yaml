apiVersion: apps/v1
kind: Deployment
metadata:
  name: yas-media
  namespace: default
  annotations:
    # 1. Chỉ định image cần theo dõi
    argocd-image-updater.argoproj.io/image-list: media=longle2112/yas-media
    # 2. Chiến lược: Luôn lấy bản mới nhất dựa trên thời gian build
    argocd-image-updater.argoproj.io/media.update-strategy: latest
    # 3. Cho phép tự động cập nhật tag vào Deployment này
    argocd-image-updater.argoproj.io/write-back-method: git
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yas-media
  template:
    metadata:
      labels:
        app: yas-media
    spec:
      containers:
      - name: yas-media
        image: longle2112/yas-media:latest # Đây là giá trị khởi tạo
        env:
          - name: SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE
            value: "5"
          - name: SPRING_DATASOURCE_URL
            value: jdbc:postgresql://postgres-service:5432/yas_media
          - name: SPRING_DATASOURCE_USERNAME
            value: user
          - name: SPRING_DATASOURCE_PASSWORD
            value: password
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: yas-media-service
spec:
  type: NodePort
  selector:
    app: yas-media
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30005 # Cổng để bạn truy cập từ IP Master