apiVersion: apps/v1
kind: Deployment
metadata:
  name: yas-payment-paypal
  namespace: default
  annotations:
    # 1. Chỉ định image cần theo dõi
    argocd-image-updater.argoproj.io/image-list: payment-paypal=longle2112/yas-payment-paypal
    # 2. Chiến lược: Luôn lấy bản mới nhất dựa trên thời gian build
    argocd-image-updater.argoproj.io/payment-paypal.update-strategy: latest
    # 3. Cho phép tự động cập nhật tag vào Deployment này
    argocd-image-updater.argoproj.io/write-back-method: git
spec:
  replicas: 0
  selector:
    matchLabels:
      app: yas-payment-paypal
  template:
    metadata:
      labels:
        app: yas-payment-paypal
    spec:
      containers:
      - name: yas-payment-paypal
        image: longle2112/yas-payment-paypal:latest # Đây là giá trị khởi tạo
        env:
          - name: SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE
            value: "5"
          - name: SPRING_DATASOURCE_URL
            value: jdbc:postgresql://postgres-service:5432/yas_payment-paypal
          - name: SPRING_DATASOURCE_USERNAME
            value: user
          - name: SPRING_DATASOURCE_PASSWORD
            value: password
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: yas-payment-paypal-service
spec:
  type: NodePort
  selector:
    app: yas-payment-paypal
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007 # Cổng để bạn truy cập từ IP Master